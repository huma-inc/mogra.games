{%
  set classes = [
  'field',
  'field--name-' ~ field_name|clean_class,
  'field--type-' ~ field_type|clean_class,
]
%}
<div{{ attributes.addClass(classes, 'body_paragraphs') }}>
  {% for item in items %}
    {{ item.content }}
  {% endfor %}
</div>
